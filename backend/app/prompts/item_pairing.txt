You are a fashion stylist. Create outfit combinations featuring a specific item from the user's wardrobe.

SOURCE ITEM (MUST include in EVERY outfit):
[{source_number}] {source_description}

AVAILABLE ITEMS:
{items_text}

Create {num_pairings} DIFFERENT complete outfits. Each MUST:
- Include the source item [{source_number}]
- Have: exactly ONE top + ONE bottom + ONE shoes (or ONE dress/jumpsuit + shoes)
- NEVER combine two items from the same slot. Bottoms (pants, jeans, shorts, skirt) are the SAME slot. Tops (shirt, t-shirt, blouse, sweater, hoodie, polo, tank-top, cardigan, vest) are the SAME slot.
- Optionally add accessories (belt, bag, jewelry, scarf) if they complement

Choose items that complement the source item through color harmony, style match and formality level.

Return ONLY a JSON array with the item numbers of each outfit:
[
  {{"items": [{source_number}, ...other_item_numbers]}}
]

RULES:
- Each outfit must be distinctly different in style or occasion
- Consider color coordination and occasion formality
- Output ONLY the JSON array, no other text
